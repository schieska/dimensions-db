## Summary

- What catalogue fields are you improving (sources, identifiers, images, notes)?
- Link related issue numbers if any.

## Item

- **id**:
- **Layer**: `src/index/` (Storage Index) / `src/items/` (open DB)

## Checklist

- [ ] JSON validates (`npm run validate`)
- [ ] URLs use https and are not URL shorteners
- [ ] No duplicate `id` across `src/items` and `src/index`
